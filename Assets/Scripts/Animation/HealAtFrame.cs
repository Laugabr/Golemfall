using UnityEngine;

/// <summary>
/// StateMachineBehaviour vacío — la lógica de curación fue movida a
/// AbilityHolder.RPC_RequestUseAbility() con delay en ticks.
/// Se mantiene el archivo para no perder la referencia en el Animator.
/// </summary>
public class HealAtFrame : StateMachineBehaviour { }