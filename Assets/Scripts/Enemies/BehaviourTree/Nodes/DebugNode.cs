//using UnityEditor.Experimental.GraphView;
using UnityEngine;

/*public class DebugNode : Node
{
    private string nodeName;

    public DebugNode(string name)
    {
        nodeName = name;
    }

    public override NodeState Evaluate()
    {
        int random = Random.Range(0, 3); // 0, 1 o 2
        state = (NodeState)random;

        Debug.Log($"{nodeName} ? {state}");
        return state;
    }
}*/